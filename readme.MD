You may have to update the following configs to make it run on your env.

Update the db.properties
	jdbc.url
	jdbc.user
	jdbc.password
Update the JDBCconnector(jar file)
Create related Databases,tables,columns,fields...